
## Important Note 

When working with the database, please be very cautious about the commands you run. Specifically, avoid using `reset` and `migrate` commands as they can lead to data loss by destroying the database.
why are you running reset and migrate, never in your life run reset or migrate

dont you know that those commands destroy the database 

you can run db commands but the only commands that you can use to run should be the generate command and the db push command. because there might be a need to add some necesary fileds in our db wehre necesary and it is okay for us to add such but we should never run migrate or reset never.

