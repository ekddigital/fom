import { prayerFastingMetadata } from "@/lib/seo/public-pages";

export const metadata = prayerFastingMetadata;

export default function PrayerFastingLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
